name: Build any rocm

on:
  workflow_call:
    inputs: &common_inputs
      filter:
        type: string
        default: 'rocm'
      deploy:
        type: boolean
      llamacpp_repo:
        type: string
      llamacpp_version:
        type: string
      boringssl_repo:
        type: string
      boringssl_version:
        type: string

  workflow_dispatch:
    inputs: *common_inputs

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      ROCM_VERSION: "10.1.0a20260822"

    steps:
      - uses: actions/checkout@v5

      - name: Install deps
        env:
          FILTER: ${{ inputs.filter }}
        run: |
          GFX="$FILTER"
          GFX="${GFX##*-}"
          case "$GFX" in
          (probe) ROCM="rocm[devel,libraries]" ;;
          (gfx*)  ROCM="rocm[devel,libraries,device-${GFX}]" ;;
          (*)     ROCM="rocm[devel,libraries,device-all]" ;;
          esac
          pip install --extra-index-url https://rocm.nightlies.amd.com/whl-multi-arch/ "$ROCM==$ROCM_VERSION"
          rocm-sdk init
          echo "ROCM_PATH=$(rocm-sdk path --root)" >> "$GITHUB_ENV"

      - name: Build ${{ inputs.filter }}
        env:
          LLAMACPP_REPO: ${{ inputs.llamacpp_repo }}
          LLAMACPP_VERSION: ${{ inputs.llamacpp_version }}
          BORINGSSL_REPO: ${{ inputs.boringssl_repo }}
          BORINGSSL_VERSION: ${{ inputs.boringssl_version }}
          HF_TOKEN: ${{ secrets.HF_TOKEN_READ }}
          FILTER: ${{ inputs.filter }}
        run: cmake --workflow --preset "$FILTER"

      - name: Deploy
        if: ${{ inputs.deploy && vars.HF_REPO != '' }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_REPO: ${{ vars.HF_REPO }}
          VERSION: ${{ inputs.llamacpp_version || 'test' }}
        run: |
          pip install -U "huggingface_hub[hf_xet]"
          python scripts/upload.py "$HF_REPO/$VERSION"
